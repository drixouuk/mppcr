# Versions figées sur celles du conteneur de référence (relevées via
# « docker exec mpp pip freeze » sur l'image fonctionnelle), Python 3.11.
# mpxj et jpype1 sont les deux dépendances sensibles : mpxj embarque les
# classes Java, jpype1 fait le pont Python/JVM via une extension C compilée.
Flask==3.1.3
gunicorn==26.2.0
# mpxj 16.7.0 est écartée : régression dans TimephasedDataFactory, où un élément
# de travail daté incohérent (début postérieur à la fin) déclenche une allocation
# non bornée — l'analyse échoue en OutOfMemoryError quelle que soit la mémoire
# allouée, y compris 6 Go, sur un .mpp de moins de 2 Mo. La 16.6.0 rejette
# proprement le même élément. Voir joniles/mpxj#932, fermée le 15/09/2026 sans
# correctif faute de fichier de reproduction : ne pas remonter à 16.7.0 tant
# qu'une version ultérieure n'est pas publiée puis vérifiée sur un fichier réel.
mpxj==16.6.0
JPype1==1.7.1
numpy==2.4.6
networkx==3.6.1
# Export Excel : pur Python, aucune sous-dépendance, wheel universelle.
XlsxWriter==3.2.9
