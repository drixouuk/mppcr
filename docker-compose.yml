services:
  mpp:
    build: .
    container_name: mpp
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      ANALYSIS_TIMEOUT: "900"
      DAILY_ANALYSIS_LIMIT: "5"
      MAX_CONCURRENT_ANALYSES: "2"
